# Required for step2 and step3.
TOGETHER_API_KEY=

# Optional. Defaults to meta-llama/Llama-3.2-3B-Instruct-Turbo (cheap, plentiful).
# Do NOT use GLM 5.1 here — it's already at capacity and will produce 429s
# for capacity reasons, not host-policy reasons.
# TOGETHER_MODEL=meta-llama/Llama-3.2-3B-Instruct-Turbo

# Optional knobs for step3-loadtest.ts.
# CONCURRENCY=10
# REQUESTS_PER_HOST=50
# INTERLEAVED_TOTAL=50
